{
  "compilerOptions": {
    "target": "ES2022",                /* Define a versão do JavaScript para a qual o código será compilado. */
    "module": "NodeNext",               /* O sistema de módulos a ser usado. ESSENCIAL para o Node.js moderno. */
    "moduleResolution": "NodeNext",     /* Como o TypeScript encontra os módulos. ESSENCIAL. */
    "baseUrl": ".",                     /* Diretório base para resolver caminhos não relativos. */
    "outDir": "./dist",                 /* Pasta onde o código JavaScript compilado será salvo. */
    "esModuleInterop": true,            /* Permite interoperabilidade com módulos CommonJS. */
    "forceConsistentCasingInFileNames": true, /* Garante que os nomes dos arquivos sejam consistentes. */
    "strict": true,                     /* Habilita todas as verificações de tipo estritas. */
    "skipLibCheck": true                /* Pula a verificação de tipos de arquivos de declaração (.d.ts). */
  },
  "include": ["src/**/*"],              /* Diz ao compilador para incluir todos os arquivos dentro da pasta 'src'. */
  "exclude": ["node_modules"]           /* Ignora a pasta 'node_modules'. */
}